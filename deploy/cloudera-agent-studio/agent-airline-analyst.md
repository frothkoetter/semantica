# Agent Studio — Airline Ontology Analyst (deprecated single-agent)

Use the **multi-agent workflow** instead: [`multi-agent-workflow.md`](multi-agent-workflow.md).

Single-agent mode mixes ontology and SQL in one role and tends to bypass the graph.
Phase 2 assigns **semantica** (ontology) and **iceberg-hive** (SQL) to separate agents.
